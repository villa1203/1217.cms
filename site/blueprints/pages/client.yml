title: Client
icon: tag
num: 0

create:
  status: listed

options:
  changeStatus: false
  changeTemplate: false
  preview: false

columns:
  main:
    width: 1/1
    fields:
      logo:
        type: files
        multiple: false
        label: Logo du client
        uploads:
          template: blocks/image
        query: model.images
        max: 1
      logo_negative:
        type: files
        multiple: false
        label: Logo du client
        uploads:
          template: blocks/image
        query: model.images
        max: 1
